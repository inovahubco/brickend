# <%= names.pascalCase %> - Git Ignore Configuration
# Generated by Brickend on <%= now() %>

# ==============================================================================
# Dependencies
# ==============================================================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-store/
<% if (variables.packageManager === 'bun') { %>
bun.lockb
<% } %>

# ==============================================================================
# Build & Distribution
# ==============================================================================
dist/
build/
out/
.next/
.nuxt/
.vuepress/dist/
*.tsbuildinfo
tsconfig.tsbuildinfo
<% if (variables.bundler === 'webpack') { %>
webpack-stats.json
<% } else if (variables.bundler === 'rollup') { %>
rollup.config.*.js
<% } %>

# ==============================================================================
# Runtime & Logs
# ==============================================================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-store/

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov
.nyc_output/

# ==============================================================================
# Environment & Configuration
# ==============================================================================
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
.envrc

# ==============================================================================
# IDE & Editor Files
# ==============================================================================
# Visual Studio Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
out/

# Sublime Text
*.sublime-workspace
*.sublime-project

# Vim
*.swp
*.swo
*~

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# ==============================================================================
# Operating System Files
# ==============================================================================
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# Linux
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# ==============================================================================
# Development & Testing
# ==============================================================================
<% if (variables.testRunner === 'jest' || !variables.testRunner) { %>
# Jest
jest.config.*.js
coverage/
.nyc_output/
<% } else if (variables.testRunner === 'vitest') { %>
# Vitest
coverage/
.vitest/
<% } %>

<% if (variables.e2eTests) { %>
<% if (variables.e2eRunner === 'playwright') { %>
# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
<% } else if (variables.e2eRunner === 'cypress') { %>
# Cypress
cypress/videos/
cypress/screenshots/
cypress/downloads/
<% } %>
<% } %>

# ==============================================================================
# Database & Storage
# ==============================================================================
<% if (variables.database === 'sqlite') { %>
# SQLite
*.sqlite
*.sqlite3
*.db
<% } %>

<% if (variables.database === 'prisma') { %>
# Prisma
prisma/migrations/
<% } %>

# Database dumps
*.sql
*.dump

# ==============================================================================
# Docker & Containerization
# ==============================================================================
<% if (variables.docker) { %>
# Docker
.dockerignore
docker-compose.override.yml
.docker/

# Container volumes
volumes/
<% } %>

# ==============================================================================
# Cloud & Deployment
# ==============================================================================
<% if (variables.deploy === 'vercel') { %>
# Vercel
.vercel
<% } else if (variables.deploy === 'netlify') { %>
# Netlify
.netlify/
<% } else if (variables.deploy === 'aws') { %>
# AWS
.aws-sam/
samconfig.toml
<% } else if (variables.deploy === 'firebase') { %>
# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
<% } %>

<% if (variables.terraform) { %>
# Terraform
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.terraform/
.terraform.lock.hcl
<% } %>

# ==============================================================================
# Runtime Specific
# ==============================================================================
<% if (variables.runtime === 'deno') { %>
# Deno
.deno/
<% } %>

<% if (variables.runtime === 'bun') { %>
# Bun
bun.lockb
<% } %>

<% if (variables.runtime === 'cloudflare') { %>
# Cloudflare Workers
.wrangler/
wrangler.toml.bak
<% } %>

# ==============================================================================
# Monitoring & Analytics
# ==============================================================================
<% if (variables.monitoring) { %>
# Monitoring
.prometheus/
grafana/data/
<% } %>

<% if (variables.sentry) { %>
# Sentry
.sentryclirc
<% } %>

# ==============================================================================
# Security & Secrets
# ==============================================================================
# Certificate files
*.pem
*.key
*.crt
*.p12
*.pfx

# SSH keys
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# GPG keys
*.gpg
*.asc
secring.*

# Credentials & tokens
credentials.json
service-account*.json
.gcp-keys.json

# ==============================================================================
# Temporary & Cache Files
# ==============================================================================
# Temporary directories
tmp/
temp/
.tmp/
.temp/

# Cache directories
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
.next/cache/

<% if (variables.packageManager === 'yarn') { %>
# Yarn
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
<% } %>

<% if (variables.packageManager === 'pnpm') { %>
# PNPM
.pnpm-store/
<% } %>

# ==============================================================================
# Static Assets & Uploads
# ==============================================================================
<% if (variables.staticAssets) { %>
# Generated static assets
public/dist/
public/build/
static/dist/
static/build/
<% } %>

# Uploaded files
uploads/
<% if (variables.includeUploads === false) { %>
public/uploads/
static/uploads/
<% } %>

# ==============================================================================
# Documentation & Reports
# ==============================================================================
# Generated documentation
docs/dist/
docs/build/
.docusaurus/
.vuepress/dist/

# Reports
reports/
lighthouse/
bundle-analyzer/

# ==============================================================================
# Backup & Archive Files
# ==============================================================================
*.bak
*.backup
*.old
*.orig
*.rej
*.save
*.swp
*.tmp

# Archive files
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# ==============================================================================
# Project Specific
# ==============================================================================
<% if (variables.customIgnores) { %>
<% variables.customIgnores.forEach(function(ignore) { %>
<%= ignore %>
<% }); %>
<% } %>

# ==============================================================================
# Local Development
# ==============================================================================
# Local configuration overrides
config.local.js
config.local.json
settings.local.json

# Development databases
dev.db
development.sqlite
test.db

# Local scripts
scripts/local/
bin/local/

# ==============================================================================
# Generated Files (keep at end)
# ==============================================================================
<% if (variables.projectType === 'library') { %>
# Generated type definitions
*.d.ts.map
<% } %>

# Auto-generated files
# (Add patterns for files generated by your build process)
<% if (variables.generateIgnoreComment) { %>

# TODO: Add project-specific generated files here
# Example: generated-api-client/
# Example: auto-generated-schemas/
<% } %>

# ==============================================================================
# Keep certain files (negative patterns)
# ==============================================================================
<% if (variables.keepFiles && variables.keepFiles.length > 0) { %>
<% variables.keepFiles.forEach(function(keepFile) { %>
!<%= keepFile %>
<% }); %>
<% } %>

# Always keep these important files
!.gitkeep
!.gitignore
!.env.example
!.env.template